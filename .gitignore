*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
# Secrets (Guideline §9 — never commit)
secrets.properties
.kotlin

# Relay per-task scratch (runbook itself is kept)
.claude/relay/*/