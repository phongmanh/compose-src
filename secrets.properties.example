# Template for secrets.properties. Copy this file to `secrets.properties` and fill in
# real values; the build injects them into BuildConfig. Never commit real secrets
# (Guideline §9 — no secrets/API keys in version control).
GATEWAY_KEY=
GATEWAY_CLIENT_ID=
GATEWAY_CLIENT_SECRET=
